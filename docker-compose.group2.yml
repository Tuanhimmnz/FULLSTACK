services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      - "${SQLSERVER_PORT:-14333}:1433"
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD:-ProjectHub@2026}
    volumes:
      - projecthub-group2-sql-data:/var/opt/mssql

  task-service:
    build:
      context: ./Microservices/TaskService
    ports:
      - "${TASK_SERVICE_PORT:-5002}:5002"
    environment:
      ASPNETCORE_URLS: http://+:5002
      ConnectionStrings__TaskDb: "Server=${SQLSERVER_HOST:-sqlserver},1433;Database=TaskDB;User Id=sa;Password=${MSSQL_SA_PASSWORD:-ProjectHub@2026};TrustServerCertificate=True;"
      Jwt__Secret: ${JWT_SECRET:-ProjectHub.Shared.Secret.Key.For.Student.Microservices.2026!}
      Jwt__Issuer: ${JWT_ISSUER:-ProjectHub}
      Jwt__Audience: ${JWT_AUDIENCE:-ProjectHub.Client}
      Services__NotifyService: ${NOTIFY_SERVICE_URL:-http://notify-service:5003}
    depends_on:
      - sqlserver
    restart: unless-stopped

volumes:
  projecthub-group2-sql-data:
